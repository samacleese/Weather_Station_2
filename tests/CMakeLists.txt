# CMakeLists.txt
# ABOUTME: Top-level CMake entry point for device test sketches.
# ABOUTME: Each subdirectory defines an independent, flashable test target.

add_subdirectory(GrayscaleCalibration)
add_subdirectory(device)
